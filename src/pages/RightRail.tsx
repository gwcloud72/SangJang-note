import { CalendarCard, IPOCalendar } from '../components/feature/sang';
import { Card, SectionHeader } from '../components/common/ui';
import type { SangData } from '../data/normalize';
export function RightRail({ data, onTabChange }: { data: SangData; onTabChange: (tab: string) => void }) { return <aside className="space-y-ds-2"><IPOCalendar companies={data.companies} compact onSelect={() => onTabChange('calendar')}/><Card padding="normal"><SectionHeader title="최근 업데이트" action="더보기" onAction={()=>onTabChange('filings')}/><div className="space-y-2">{data.companies.slice(0,5).map((company)=><CalendarCard key={company.id} company={company} onOpen={()=>onTabChange('filings')}/>)}</div></Card><Card padding="normal"><SectionHeader title="오늘의 체크포인트"/><ul className="space-y-2 text-sm text-ink-700"><li className="rounded-md bg-ink-50 px-ds-2 py-ds-1">청약 마감 시간 확인</li><li className="rounded-md bg-ink-50 px-ds-2 py-ds-1">공시 원문 비교</li><li className="rounded-md bg-ink-50 px-ds-2 py-ds-1">상장일 변동성 점검</li></ul></Card></aside>; }
